# How many types of data structure are there?
- **There are three types of data structure**
    - **Series**
        - One-dimensional array capable of storing various data types
        ```
        import pandas as pd
        a=pd.Series()
        print(a)
        ```
        
